import { FormateDatePipe } from './formate-date.pipe';

describe('FormateDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormateDatePipe();
    expect(pipe).toBeTruthy();
  });
});
